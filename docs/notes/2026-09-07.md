# Working notes, 2026-09-07

- Seed `20260907` produced mode roll `0.9282518865358783`, selecting a meaningful improvement to an existing project.
- ReachGrid was selected in the Android lane. It was not improved in the previous four scheduled runs, its repository was clean and synchronized, and its 1.0 documentation named paired comparison plus opt-in local history as the best next improvement.
- Public and local portfolio inspection found no other current project with the same native Android, direct-touch, private paired-measurement workflow.
- The selected design keeps both phases on the same route and handedness, derives phase order reproducibly from the seed, and labels every comparison as second minus first.
- The local archive is off by default, bounded at 20 completed sessions, tolerant of damaged stored lines, explicitly shareable, and explicitly clearable.
- No internet permission, account, telemetry, background work, raw touch coordinates, model, or external dataset was added.
- JDK 8 locally compiled the expanded dependency-free core with all lint warnings enabled.
- The SDK-free harness passed 8/8 groups. Its 1,000-seed order check assigned the lower radius first 526 times. This is a reproducibility and broad-balance check, not a statistical validation of the human-subject protocol.
- A deterministic two-report fixture produced exactly `-100 ms`, `+10 reach-score points`, and `0 misses` for second minus first across all 24 cells. The comparison JSON parsed with 24 deltas and 48 samples in each nested report.
- Android UI compilation, lint, tests, and APK assembly require public CI because the local machine has JDK 8 but no Android SDK.
