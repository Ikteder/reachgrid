# Working notes, 2026-08-29

- The user requested an additional manual project after the portfolio was narrowed. RelayFault had already been completed earlier the same day, so this run is recorded as a supplemental manual run rather than a missing scheduled trigger.
- Seed `20260829` produced mode roll `0.6032413721990276`, selecting new project.
- Recent work maps to the software-engineering lane, so focused category 2 was excluded. The eligible list was `[1, 3, 4, 5]`; uniform index `2` selected category 4, Android applications.
- The local machine had Java 8 but no Android SDK, Android Studio, Gradle, Kotlin, Dart, or Flutter installation.
- Native Java was selected because the core could be verified locally with the available JDK and the real Android package could be built in public CI. Flutter was not chosen merely to mention a framework.
- Android Gradle Plugin 9.3.0, Gradle 9.5.0, JDK 17, SDK platform 36, and Build Tools 36.0.0 were pinned from the [official Android Gradle Plugin 9.3 release notes](https://developer.android.com/build/releases/agp-9-3-0-release-notes).
- Local core verification passed deterministic routing, balanced coverage, miss attribution, exact completion, summary aggregation, score bounds, and JSON parsing.
- The generated report contained schema version 1, 48 samples, 24 cells, 6 injected misses, and no raw touch-coordinate fields.
- The local environment could not compile the Android UI or run an emulator. That limitation is preserved rather than described as device verification.
- All new README files were checked for the saved punctuation preference.
